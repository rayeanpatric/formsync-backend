# Redis Configuration for Collaborative Form System
# Add these to your .env file for Redis integration

# Uncomment and configure these lines to enable Redis for better performance:
# REDIS_URL=redis://localhost:6379
# REDIS_HOST=localhost
# REDIS_PORT=6379

# For Upstash Redis (cloud Redis):
# REDIS_URL=rediss://your-upstash-url

# For Redis Cloud or other hosted Redis:
# REDIS_URL=redis://username:password@host:port

# Note: The system will automatically fall back to in-memory storage if Redis is unavailable
